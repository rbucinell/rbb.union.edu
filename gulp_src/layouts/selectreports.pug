extends partials/layout.pug
block config
    - const [menu,name] = ['Scholarship', 'Select Reports'];
block scripts 
    script.
        document.addEventListener('DadContentLoaded', ()=> formatListGroup('Senior Project and Thesis Reports'));
