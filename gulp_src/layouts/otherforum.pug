extends partials/layout.pug
block config
    - const [menu,name] = ['Student Collaborations', 'Other Forums']
block scripts 
    script.
        document.addEventListener('DadContentLoaded', ()=> formatListGroup('Other Forums'));