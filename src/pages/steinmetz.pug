extends partials/layout.pug
block config
    - const [menu,name] = ['Student Collaborations', 'Steinmetz Symposium']
block scripts 
    script.
        document.addEventListener('DadContentLoaded', ()=> formatListGroup('Steinmetz Symposium Presentations'));