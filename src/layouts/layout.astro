---
import '@fontsource/cabin';
import "../styles/rbb.css";
import Header from 	'@components/layout/header.astro';
import Head from 	'@components/layout/head.astro';
import Sidebar from '@components/sidebar/sidebar.astro';
import Content from '@components/layout/content.astro';

interface Props {
	category?:string;
	slug: string;
	name: string;
	description?: string;
}

const { category, slug, name, description } = Astro.props;
---
<!doctype html>
<html lang="en">
	<Head name={name} description={description} />
	<body class="min-h-screen grid grid-rows-[auto,1fr,auto]">
		<Sidebar>
			<!-- https://www.transparenttextures.com/ -->
			<main class="flex w-full h-screen bg-gray-200 dark:bg-rbb-gray-800 bg-[url('./img/texture/otis-redding.png')]">
				<div class="flex flex-col flex-1 overflow-y-auto">
					<Header slug={slug} category={category} name={name}/>
					<Content name={name} href={slug} >
						<slot/>
					</Content>
				</div>
			</main>
		</Sidebar>
	</body>
</html>