extends partials/layout.pug
block config
    - const [menu,name] = ['Scholarship','Addresses & Speeches'];
block scripts 
    script.
        document.addEventListener('DadContentLoaded', ()=> formatListGroup('Addresses & Speeches'));